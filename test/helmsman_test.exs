defmodule HelmsmanTest do
  use ExUnit.Case, async: true
  doctest Helmsman

  # test "Expect Helmsman to map processor Spec to Operation"
  # test "Expect Helmsman to combine Operations into flat pipeline"
  # test "Expect Helmsman to combine Operations into DGU"

end
